name: Instrumented tests

# Instrumented tests need an Android emulator; they're split into a separate
# workflow from the unit tests so a unit-test-only CI cycle stays fast.
#
# Uses the `reactivecircus/android-emulator-runner` action which boots an AVD
# in-CI. Runs the behavioral tests (AlarmFlow, BootFlow, AlertService,
# Acknowledge, SettingsPersistence) and the OverlayInstrumentedTest UI test.
# Excludes (via notClass) only the screenshot-capture tests (AppScreenshots and
# ScreenshotCI) — those are Fastlane-driven (the screenshots workflow) and
# assert nothing on their own, so there's no reason to run them here.
#
# The googlePlay flavor runs across the full API matrix; the fDroid and
# fDroidLite flavors (android.location LocationHelper) each additionally run
# once on the newest API.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  instrumented-tests:
    runs-on: ubuntu-latest
    # AVD boot + Robolectric-free tests can take 10–15 min on free runners.
    timeout-minutes: 30

    strategy:
      # Don't cancel siblings if one API fails — we want to see every break.
      fail-fast: false
      matrix:
        # API levels span the version-sensitive branches in the codebase:
        #   22 — minSdk; pre-M alarm scheduling, no notification channels,
        #        no runtime permissions, no Direct Boot
        #   28 — P; keyguard-based activity detection enables
        #   33 — T; POST_NOTIFICATIONS runtime perm, Geocoder async, FOREGROUND_SERVICE_DATA_SYNC
        #   34 — U; FOREGROUND_SERVICE_TYPE_SHORT_SERVICE required on AlertService
        #   35 — V; current targetSdk
        api-level: [22, 28, 33, 34, 35]
        flavor: [GooglePlay]
        include:
          # also exercise the android.location-based fDroid LocationHelper
          # on the current targetSdk — it is otherwise only covered by
          # Robolectric unit tests
          - api-level: 35
            flavor: FDroid
          # smoke-test the fDroidLite build on a device too: it ships a separate
          # applicationId and INCLUDE_WEBHOOK=false, and reuses fDroid's
          # android.location code. Otherwise only covered by unit tests.
          - api-level: 35
            flavor: FDroidLite

    steps:
      - uses: actions/checkout@v6

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '21'
          cache: 'gradle'

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Run instrumented tests (skipping screenshot flow)
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: ${{ matrix.api-level }}
          # google_apis gives us Play Services on-emulator for the
          # googlePlay flavor's FusedLocationProviderClient. `default` is
          # the only reliable image for older levels (API 22).
          target: ${{ matrix.api-level < 28 && 'default' || 'google_apis' }}
          arch: x86_64
          # A real device profile is needed for the UI test (Overlay): the bare
          # default AVD boots a tiny 320x640 screen whose window never gains
          # focus on a headless runner, so Espresso/UiAutomator can't see the
          # overlay views even though it renders ("Overlay shown" logs).
          # pixel_7 is the same profile the screenshots workflow uses
          # successfully on a headless emulator. Gated to API 28+ (Overlay is
          # API 26+ anyway) — older system images don't reliably ship modern
          # device profiles (an earlier pixel_8 attempt failed AVD creation on
          # API 22), so API 22 keeps the runner's default device.
          profile: ${{ matrix.api-level >= 28 && 'pixel_7' || '' }}
          disable-animations: true
          # The script runs after the AVD is created and the emulator booted.
          # Logging `avdmanager list devices` first surfaces which device
          # profiles are actually available on this runner's cmdline-tools,
          # so if we ever want to set `profile:` again we know what's safe.
          #
          # NOTE: emulator-runner runs EACH LINE as its own `sh -c` call.
          # That means line continuations (`\`) don't work — the gradle
          # invocation must fit on one physical line.
          script: |
            echo "=== Available device profiles on API ${{ matrix.api-level }} ==="
            avdmanager list devices -c | sort -u | head -40
            echo "==="
            ./gradlew connected${{ matrix.flavor }}DebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.notClass=io.keepalive.android.AppScreenshotsInstrumentedTest,io.keepalive.android.ScreenshotCIInstrumentedTest --no-daemon

      - name: Upload instrumented test report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: instrumented-test-report-api${{ matrix.api-level }}-${{ matrix.flavor }}
          path: |
            app/build/reports/androidTests/
            app/build/outputs/androidTest-results/
